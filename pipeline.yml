#  Template NodeJS build
#
#  This template allows you to validate your NodeJS code.
#  The workflow allows running tests and code linting on the default branch.

image: node:15.10.0

pipelines:
  default:
    - parallel:
        - step:
            name: Install New man
            caches:
              - node
            script:
              - npm install curl
              - curl --request get http://34.195.140.114:8080/job/Vibes/build?token=114c7863a1c3b087e61b6a816bf1966ed5 --user "anandn007:anand.n007"
